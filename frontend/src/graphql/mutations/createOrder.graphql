mutation CreateOrder($input: OrderInput!) {
  createOrder(input: $input) {
    id
    userId
    paymentStatus
    shippingStatus
    products {
      id
      quantity
    }
    updatedAt
    createdAt
  }
}
