query GetFilteredProducts($query: String, $categoryId: Int, $orderBy: String) {
  getFilteredProducts(
    query: $query
    categoryId: $categoryId
    orderBy: $orderBy
  ) {
    ...ProductProps
  }
}
